// file should be named as .jsx to be recognized as a React component
// function name also should be capitalized to be recognized as a React component

// in vite we can use by default .jsx extension for all files and it will work fine. but it is a good practice to use .jsx extension for files that return html elements and .js extension for files that return javascript code.

function Chai() {
    return (
        <h3>Chai is ready!</h3>
    )
}

export default Chai;