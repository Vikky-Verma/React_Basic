function Chai() {
    return (
        <h2> chai in react  </h2>
    )
}

export default Chai;

// function should recommend to naming as capatalize because it is a component.
// if we use small letter then it will be treated as html element and it will not work.

// where html element return then we use .jsx file name and where we return component(javascript code) then we use .js file name. it is a convention but it is not mandatory. we can use .js file name for both html element and component but it is better to follow the convention.